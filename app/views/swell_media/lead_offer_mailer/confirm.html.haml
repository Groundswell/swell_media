!!!strict
%head
	%meta{ content: "text/html; charset=UTF-8", 'http-equiv' => "Content-Type" }
%body
	%p
		= raw @offer.email_msg

	- if @offer.download_url.present?
		%p
			You can get your
			= @offer.item_name
			here:
			%a{ href: @offer.download_url }
				= @offer.download_url