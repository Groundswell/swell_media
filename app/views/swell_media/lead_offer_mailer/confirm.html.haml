!!!strict
%head
	%meta{ content: "text/html; charset=UTF-8", 'http-equiv' => "Content-Type" }
%body
	%p
		= raw @offer.email_msg

	- if @offer.download_url.present?
		%p
			You can get your
			= @offer.item_name
			here:
			%a{ href: accept_lead_offer_path( id: @offer_optin.optin.code ) }
				= accept_lead_offer_path( id: @offer_optin.optin.code )